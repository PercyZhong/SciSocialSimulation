{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "SciMirror mock experiment",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/run.py",
      "args": [
        "run",
        "--config",
        "configs/mock.json"
      ],
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}